This is a stub types definition for terminal-image (https://github.com/sindresorhus/terminal-image).

terminal-image provides its own type definitions, so you don't need @types/terminal-image installed!